# ConsoleAtHome

The console framework we found in the back of the fridge and thought "Yeah, this is still good to eat!".

## What is this?

ConsoleAtHome is simply an opinionated framework that allows for easier input parsing and menu-state navigation in default C# console apps.  
It's a low-fidelity framework (hence the name) meant to be used within a school environment.
